const CLOUD_CONFIG_KEY='smartprice_cloud_config_v1';
const CLOUD_STATUS_KEY='smartprice_cloud_status_v1';
const MAIN_STORE_ID='00000000-0000-0000-0000-000000000001';
const ACTIVE_STORE_KEY='smartprice_active_store_v1';

window.SmartPriceCloud={
  version:'1.6',_client:null,_signature:'',
  normalizeUrl(value){let raw=String(value||'').trim();if(!raw)return'';if(!/^https?:\/\//i.test(raw))raw='https://'+raw;raw=raw.replace(/\/rest\/v1\/?(?:.*)?$/i,'').replace(/\/+$/,'');try{const u=new URL(raw);return`${u.protocol}//${u.host}`}catch{return raw}},
  config(){try{const saved=JSON.parse(localStorage.getItem(CLOUD_CONFIG_KEY)||'{}');return{enabled:false,url:'',key:'',...saved,url:this.normalizeUrl(saved.url)}}catch{return{enabled:false,url:'',key:''}}},
  saveConfig(c){const clean={enabled:!!c.enabled,url:this.normalizeUrl(c.url),key:String(c.key||'').trim()};localStorage.setItem(CLOUD_CONFIG_KEY,JSON.stringify(clean));this._client=null;this._signature='';return clean},
  validate(c){if(!c.url||!c.key)throw new Error('URL Supabase ou clé publique absente.');const url=this.normalizeUrl(c.url);if(!/^https:\/\/[a-z0-9-]+\.supabase\.co$/i.test(url))throw new Error(`URL invalide : ${url}. Utilisez uniquement https://xxxxx.supabase.co`);if(!/^sb_publishable_/i.test(c.key)&&!/^eyJ/i.test(c.key))throw new Error('Clé publique invalide ou incomplète.');if(!window.supabase?.createClient)throw new Error('Le SDK Supabase n’est pas chargé. Vérifiez Internet et rechargez la page.')},
  client(c=this.config()){c={...c,url:this.normalizeUrl(c.url)};this.validate(c);const sig=c.url+'|'+c.key;if(!this._client||this._signature!==sig){this._client=window.supabase.createClient(c.url,c.key,{auth:{persistSession:false,autoRefreshToken:false,detectSessionInUrl:false},global:{headers:{'X-Client-Info':'caissekom-smartprice/1.6'}}});this._signature=sig}return this._client},
  explain(error,prefix='Erreur Supabase'){if(!error)return prefix;const code=error.code?` [${error.code}]`:'';let msg=error.message||String(error);if(error.code==='PGRST205'||/schema cache|Could not find the table/i.test(msg))msg='La table demandée n’est pas visible dans l’API. Exécutez la migration SQL v1.2.';else if(error.code==='42501'||/permission denied|row-level security/i.test(msg))msg='Accès refusé par les règles RLS. Exécutez la migration SQL v1.2.';else if(/Failed to fetch|NetworkError/i.test(msg))msg='Connexion réseau impossible. Vérifiez Internet, l’URL du projet et les bloqueurs de contenu.';else if(/Invalid API key|JWT/i.test(msg))msg='Clé publique invalide ou copiée incomplètement.';return`${prefix}${code} : ${msg}`},
  activeStoreId(){return localStorage.getItem(ACTIVE_STORE_KEY)||MAIN_STORE_ID},
  setActiveStoreId(id){localStorage.setItem(ACTIVE_STORE_KEY,String(id||MAIN_STORE_ID));return this.activeStoreId()},
  async test(c=this.config()){const db=this.client(c),storeId=this.activeStoreId();let r=await db.from('store_products').select('barcode',{count:'exact',head:true}).eq('store_id',storeId);if(r.error&&r.error.code==='PGRST205')r=await db.from('products').select('barcode',{count:'exact',head:true});if(r.error)throw new Error(this.explain(r.error,'Connexion refusée'));return{ok:true,count:r.count||0,url:this.normalizeUrl(c.url),version:this.version,storeId}},
  async pullArticles(c=this.config()){const db=this.client(c),all=[],storeId=this.activeStoreId();let from=0,size=1000,useLegacy=false;while(true){let q=useLegacy?db.from('products').select('barcode,designation,price,updated_at').eq('active',true):db.from('store_products').select('barcode,designation,price,updated_at').eq('store_id',storeId).eq('active',true);const{data,error}=await q.order('designation',{ascending:true}).range(from,from+size-1);if(error&&!useLegacy&&error.code==='PGRST205'){useLegacy=true;from=0;all.length=0;continue}if(error)throw new Error(this.explain(error,'Téléchargement impossible'));all.push(...(data||[]));if(!data||data.length<size)break;from+=size}return all.map(x=>({code:String(x.barcode),designation:x.designation,prix:Number(x.price),updatedAt:x.updated_at}))},
  async getExistingMap(c=this.config()){const db=this.client(c),map=new Map(),storeId=this.activeStoreId();let from=0,size=1000;while(true){const{data,error}=await db.from('store_products').select('barcode,designation,price,active').eq('store_id',storeId).range(from,from+size-1);if(error)throw new Error(this.explain(error,'Lecture du catalogue du magasin impossible. Exécutez la migration SQL v1.4.'));for(const x of data||[])map.set(String(x.barcode),x);if(!data||data.length<size)break;from+=size}return map},
  async syncCatalog(articles,{replaceMissing=false,onProgress=null,source='manual'}={},c=this.config()){
    const db=this.client(c),now=new Date().toISOString(),storeId=this.activeStoreId(),existing=await this.getExistingMap(c),rows=articles.map(a=>({store_id:storeId,barcode:String(a.code).trim(),designation:String(a.designation||'').trim(),price:Number(a.prix)||0,active:true,updated_at:a.updatedAt||now})).filter(x=>x.barcode&&x.designation);
    let added=0,updated=0;for(const row of rows){const old=existing.get(row.barcode);if(!old)added++;else if(old.designation!==row.designation||Number(old.price)!==Number(row.price)||old.active!==true)updated++}
    for(let i=0;i<rows.length;i+=500){const{error}=await db.from('store_products').upsert(rows.slice(i,i+500),{onConflict:'store_id,barcode',ignoreDuplicates:false});if(error)throw new Error(this.explain(error,`Envoi impossible (lot ${Math.floor(i/500)+1}). Exécutez la migration SQL v1.4.`));onProgress?.(Math.min(85,Math.round(((i+500)/Math.max(rows.length,1))*85)))}
    let deactivated=0;if(replaceMissing){const incoming=new Set(rows.map(x=>x.barcode)),missing=[...existing.keys()].filter(x=>!incoming.has(x));deactivated=missing.length;for(let i=0;i<missing.length;i+=500){const{error}=await db.from('store_products').update({active:false,updated_at:now}).eq('store_id',storeId).in('barcode',missing.slice(i,i+500));if(error)throw new Error(this.explain(error,'Désactivation des anciens articles impossible'))}}
    const summary={storeId,total:rows.length,added,updated,deactivated,mode:replaceMissing?'replace':'merge',source,created_at:now};
    const{error:logError}=await db.from('imports').insert({filename:source,total_rows:rows.length,added_rows:added,updated_rows:updated,error_rows:0,deactivated_rows:deactivated,mode:summary.mode,created_at:now,store_id:storeId});if(logError)console.warn('Journal import non enregistré',logError);
    onProgress?.(100);return summary
  },
  async pushArticles(articles,c=this.config()){const r=await this.syncCatalog(articles,{replaceMissing:false,source:'manual-push'},c);return r.total},
  async listStores(c=this.config()){const db=this.client(c);const{data,error}=await db.from('stores').select('id,name,address,phone,email,website,logo,updated_at').order('name');if(error)throw new Error(this.explain(error,'Liste des magasins indisponible'));return data||[]},
  async createStore(store,c=this.config()){const db=this.client(c),row={name:String(store.name||'Nouveau magasin').trim(),address:store.address||null,phone:store.phone||null,email:store.email||null,website:store.website||null,logo:store.logo||null,updated_at:new Date().toISOString()};const{data,error}=await db.from('stores').insert(row).select().single();if(error)throw new Error(this.explain(error,'Création du magasin impossible'));return data},
  async deleteStore(id,c=this.config()){if(id===MAIN_STORE_ID)throw new Error('Le magasin principal ne peut pas être supprimé.');const db=this.client(c);const{error}=await db.from('stores').delete().eq('id',id);if(error)throw new Error(this.explain(error,'Suppression impossible'))},
  async pullSettings(c=this.config()){const db=this.client(c),id=this.activeStoreId();const{data,error}=await db.from('stores').select('id,name,address,phone,email,website,logo,updated_at').eq('id',id).maybeSingle();if(error)throw new Error(this.explain(error,'Paramètres indisponibles'));if(!data)return null;return{id:data.id,name:data.name||'',address:data.address||'',phone:data.phone||'',email:data.email||'',website:data.website||'',logo:data.logo||''}},
  async pushSettings(settings,c=this.config()){const db=this.client(c),row={id:this.activeStoreId(),name:settings.name||'SmartPrice',address:settings.address||null,phone:settings.phone||null,email:settings.email||null,website:settings.website||null,logo:settings.logo||null,updated_at:new Date().toISOString()};const{error}=await db.from('stores').upsert(row,{onConflict:'id'});if(error)throw new Error(this.explain(error,'Envoi des paramètres impossible'))},
  makeClientLink(baseUrl,c=this.config()){
    this.validate(c);const payload={v:2,url:this.normalizeUrl(c.url),key:String(c.key||'').trim(),enabled:true,storeId:this.activeStoreId()};const encoded=btoa(unescape(encodeURIComponent(JSON.stringify(payload))));const url=new URL(baseUrl,location.href);url.hash='cloud='+encodeURIComponent(encoded);return url.toString();
  },
  importConfigFromLocation(){try{const hash=location.hash||'';const match=hash.match(/(?:^#|&)cloud=([^&]+)/);if(!match)return null;const raw=decodeURIComponent(match[1]);const payload=JSON.parse(decodeURIComponent(escape(atob(raw))));const saved=this.saveConfig({enabled:true,url:payload.url,key:payload.key});if(payload.storeId)this.setActiveStoreId(payload.storeId);history.replaceState(null,'',location.pathname+location.search);return saved}catch(err){console.warn('Configuration cloud QR invalide',err);return null}},
  setStatus(ok,message){localStorage.setItem(CLOUD_STATUS_KEY,JSON.stringify({ok,message,date:new Date().toISOString(),version:this.version}))}
};
